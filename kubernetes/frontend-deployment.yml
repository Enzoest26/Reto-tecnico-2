apiVersion: apps/v1
kind: Deployment
metadata:
  name: front-deployment
  labels:
    app: app-front
spec:
  replicas: 3
  selector:
    matchLabels:
      app: app-front
  template:
    metadata:
      labels:
        app: app-front
    spec:
      containers:
        - name: app-front
          image: enzoest26/app-front:latest
          ports:
            - containerPort: 80 
