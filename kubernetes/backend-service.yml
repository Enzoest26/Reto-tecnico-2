apiVersion: v1
kind: Service
metadata:
  name: back-service
  labels:
    app: back-service
spec:
  type: ClusterIP
  selector:
    app: app-back
  ports:
    - name: http
      port: 8080
      targetPort: 8080
